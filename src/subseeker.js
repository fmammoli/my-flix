export default function createSeeker (){
  return {
    seek: function seek(magnetURI, lang) {
      return new Promise((resolve, reject) => {

        
        //transform
          //donwload bits
          //hash
          //return object
        //search

        const subtitle = {
          movieName: 'movieName',
          fileName: 'fileName',
          filePath: 'filePath',
          lang: 'lang',
          downloadLink: 'downloadLink'
        }
        resolve(subtitle)
      })
    }
  }
}